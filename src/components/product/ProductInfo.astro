---
import Button from "../Button.astro";

const { product, error } = Astro.props;
---
{
    error ? (
        <p class="text-red-500">{error}</p>
    ) : (
        <div class="border rounded-2xl bg-white/85 md:px-6 px-3 md:py-8 py-6 absolute md:bottom-[10%] bottom-[5%] md:right-[7%] right-[5%] flex flex-col md:gap-3 gap-2">
            <h2 class=" font-figtree text-clara-text-blue font-bold text-2xl">
                {product.fields.Nombre}
            </h2>
            <p class="text-clara-text-blue">{product.fields.Descripcion}</p>
            <p class="text-clara-text-blue">{product.fields.Precio}€</p>
            <Button
                text="Contacta amb nosaltres"
                className=" text-xs md:text-base"
            />
        </div>
    )
}
