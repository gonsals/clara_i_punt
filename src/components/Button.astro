---
const { text, className = "", href = "#", ...props } = Astro.props;
---

<a
    class={`px-6 py-3 rounded-[80px] font-semibold bg-clara-purple text-white cursor-pointer z-10 ${className} hover:scale-105 transition-transform duration-500`}
    {...props}
    href={href}
>
    {text}
</a>
