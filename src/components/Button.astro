---
const { text, className = "", ...props } = Astro.props;
---

<a
    class={`px-6 py-3 rounded-[80px] font-semibold bg-clara-purple text-white cursor-pointer z-10 ${className}`}
    {...props}
>
    {text}
</a>
